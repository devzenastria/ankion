# ANKION public Supabase environment placeholders.
# Copy to a local .env file only in a future approved implementation phase.
# These values are public client values, not authorization.

EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Never add service role keys, JWT secrets, storage signing secrets, or backend secrets here.